{
  "name": "cloud-demo",
  "version": "1.0.0",
  "description": "This application contains all common libraries and their usage for a web application with spring:\r * Spring Web\r * Spring Data JPA\r * Lombok\r * Mapstruct\r * Postgres DB",
  "main": "index.js",
  "scripts": {
    "load-test": "artillery run load-test-local.yaml",
    "load-test-cloud": "artillery run load-test-cloud.yaml"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "artillery": "^2.0.20"
  }
}